/**
 * Placeholder for OAuth providers (Google, Facebook, etc.)
 * Wire up when OAuth support is added to the API.
 */
export function OAuthButtons() {
  return (
    <div className="text-center text-xs text-gray-400">
      Social login coming soon
    </div>
  );
}
